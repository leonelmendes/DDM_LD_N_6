<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="iTask_App_Mobile.Views.AddProgramadorPage"
             Title="AddProgramadorPage">
    <ScrollView>
        <VerticalStackLayout Spacing="10" Padding="20">

            <!-- Header -->
            <VerticalStackLayout>
                <Label Text="Adicionar Programador"
                        Grid.Column="1"
                        FontFamily="InterSemiBold"
                        FontSize="Large"
                        VerticalOptions="Center"/>

                <Label Text="Registe um novo programador para a sua equipa."
                       FontFamily="InterLight"
                       FontSize="13"/>
            </VerticalStackLayout>

            <!-- Formulario -->
            <VerticalStackLayout Margin="0,20" Spacing="10">
                <StackLayout Spacing="5">
                    <Label Text="Nome completo"
                           FontFamily="InterSemiBold"
                           FontSize="14"
                           TextColor="{StaticResource Gray900}"/>       
                    <Frame Padding="10,3" CornerRadius="15" >
                        <Entry Placeholder="Digite o nome" FontFamily="InterRegular" PlaceholderColor="{StaticResource Gray400}"/>
                    </Frame>
                </StackLayout>


                <StackLayout Spacing="5">
                    <Label Text="Email"
                           FontFamily="InterSemiBold"
                           FontSize="14"
                           TextColor="{StaticResource Gray900}"/>
                    <Frame Padding="10,3" CornerRadius="15" >
                        <Entry Placeholder="email@exemplo.com" FontFamily="InterRegular" PlaceholderColor="{StaticResource Gray400}"/>
                    </Frame>
                </StackLayout>


                <StackLayout Spacing="5">
                    <Label Text="Username"
                           FontFamily="InterSemiBold"
                           FontSize="14"
                           TextColor="{StaticResource Gray900}"/>
                    <Frame Padding="10,3" CornerRadius="15" >
                        <Entry Placeholder="Nome de Utilizador" FontFamily="InterRegular" PlaceholderColor="{StaticResource Gray400}"/>
                    </Frame>
                </StackLayout>

                <StackLayout Spacing="5">
                    <Label Text="Password"
                           FontFamily="InterSemiBold"
                           FontSize="14"
                           TextColor="{StaticResource Gray900}"/>
                    <Frame Padding="10,3" CornerRadius="15" >
                        <Entry Placeholder="Defina uma password para o programador" FontFamily="InterRegular" PlaceholderColor="{StaticResource Gray400}"/>
                    </Frame>
                </StackLayout>

                <StackLayout Spacing="5">
                    <Label Text="Nível de experiência"
                           FontFamily="InterSemiBold"
                           FontSize="14"
                           TextColor="{StaticResource Gray900}"/>
                     <Frame CornerRadius="10"
                           BackgroundColor="{StaticResource Gray100}"
                           Padding="10,3">
                        <Grid >
                            <Picker Title="Selecione o nível" FontFamily="InterRegular" >
                                <Picker.Items>
                                    <x:String>Júnior</x:String>
                                    <x:String>Pleno</x:String>
                                    <x:String>Sênior</x:String>
                                    <x:String>Top Xuxa</x:String>
                                </Picker.Items>
                            </Picker>

                            <Image HorizontalOptions="End" Source="seta_baixo2.png" HeightRequest="20" WidthRequest="20"/>
                        </Grid>
                    </Frame>
                </StackLayout>

                <!-- Botão -->
                <Button Text="✓  Registrar Programador"
                        FontFamily="InterSemiBold"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"
                        CornerRadius="14"
                        HeightRequest="52"
                        Margin="0,10,0,20"/>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>